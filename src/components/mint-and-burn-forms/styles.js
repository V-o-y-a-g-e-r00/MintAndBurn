import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    MintAndBurnForms: {
        display:'flex',
        width:'100%',
        justifyContent: 'space-between',
        // backgrosund: 'red',
    }
})